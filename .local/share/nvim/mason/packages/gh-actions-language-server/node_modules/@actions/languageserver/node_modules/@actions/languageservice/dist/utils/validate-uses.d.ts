/**
 * Shared validation utilities for step `uses` field format.
 * Used by both workflow and action validation.
 */
import { Diagnostic } from "vscode-languageserver-types";
import { StringToken } from "@actions/workflow-parser/templates/tokens/string-token";
/**
 * Checks if a ref looks like a short SHA and adds a warning if so.
 * Returns true if a warning was added.
 */
export declare function warnIfShortSha(diagnostics: Diagnostic[], token: StringToken, ref: string): boolean;
/**
 * Validates the format of a step's `uses` field.
 *
 * Valid formats:
 * - docker://image:tag
 * - ./local/path
 * - .\local\path (Windows)
 * - {owner}/{repo}@{ref}
 * - {owner}/{repo}/{path}@{ref}
 */
export declare function validateStepUsesFormat(diagnostics: Diagnostic[], token: StringToken): void;
//# sourceMappingURL=validate-uses.d.ts.map