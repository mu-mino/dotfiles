/**
 * Checks if a cron expression specifies an interval shorter than 5 minutes.
 * GitHub Actions schedules run at most every 5 minutes, so intervals < 5 min won't work as expected.
 */
export declare function hasCronIntervalLessThan5Minutes(cron: string): boolean;
export declare function isValidCron(cron: string): boolean;
export declare function getCronDescription(cronspec: string): string | undefined;
//# sourceMappingURL=cron.d.ts.map