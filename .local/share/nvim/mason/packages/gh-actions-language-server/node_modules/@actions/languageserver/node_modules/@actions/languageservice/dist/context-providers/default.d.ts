import { data, DescriptionDictionary } from "@actions/expressions";
import { ActionContext } from "../context/action-context.js";
import { WorkflowContext } from "../context/workflow-context.js";
import { ContextProviderConfig } from "./config.js";
export type ContextValue = DescriptionDictionary | data.Null;
export declare enum Mode {
    Completion = 0,
    Validation = 1,
    Hover = 2
}
/**
 * Build expression context for workflow files (e.g., github.*, steps.*, needs.*)
 */
export declare function getWorkflowExpressionContext(names: string[], config: ContextProviderConfig | undefined, workflowContext: WorkflowContext | undefined, mode: Mode): Promise<DescriptionDictionary>;
/**
 * Get context for expression completion in action.yml files.
 * Actions have a more limited set of contexts available compared to workflows.
 */
export declare function getActionExpressionContext(names: string[], config: ContextProviderConfig | undefined, actionContext: ActionContext | undefined, mode: Mode): DescriptionDictionary;
//# sourceMappingURL=default.d.ts.map